# Root requirements — this is what Vercel's Python runtime installs for the
# serverless function in api/index.py. Keep it in step with backend/requirements.txt
# (same file, copied here because Vercel looks for it at the project root).
#
flask>=2.3.0
flask-cors>=5.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# Password hashing for admin/coordinator login (auth_service.py)
bcrypt>=4.1.0

# Local QR generation for passport emails — no third-party QR service is used
qrcode>=7.4.2
pillow>=10.0.0

# Production WSGI server. Do not run app.run() in production.
#   gunicorn --bind 0.0.0.0:5000 --workers 4 'app:app'
gunicorn>=21.2.0

# Excel export built server-side (services/export_service.py).
# Not SheetJS in the browser: every sheet is a multi-table join and the
# browser has no business holding every participant's details.
openpyxl>=3.1.0
